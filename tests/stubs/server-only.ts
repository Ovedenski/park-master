// Empty stub — replaces Next.js' `server-only` package in the Vitest runtime.
// `server-only` exists purely to throw at build time if a server module gets
// pulled into a client bundle. In a Node test runner that check is irrelevant.
export {};
